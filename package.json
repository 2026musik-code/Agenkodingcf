{
  "name": "ai-agent-coding",
  "version": "1.0.0",
  "scripts": {
    "build": "python3 build_worker.py",
    "dev": "npm run build && wrangler dev src/index.ts",
    "deploy": "npm run build && wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "wrangler": "^3.114.17"
  }
}
